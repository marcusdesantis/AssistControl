// Anchos máximos de contenido. En tablets (iPad) el layout se estiraba a todo el
// ancho de la pantalla; estos límites lo mantienen centrado y legible.
// En teléfonos no tienen efecto: el ancho disponible siempre es menor.

/** Formularios centrados — login, modales con inputs. */
export const FORM_MAX_WIDTH = 480

/** Pantallas de contenido y listas — inicio, historial, perfil, notificaciones. */
export const CONTENT_MAX_WIDTH = 900
